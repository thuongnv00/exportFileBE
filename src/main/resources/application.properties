spring.application.name=EcommerceBE

# Server port (8081 local, Render tự set PORT)
server.port=${PORT:8081}

# ========================
# Database Config
# ========================
#spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/ecommerce}
#spring.datasource.username=${DATABASE_USER:ecommerce_user}
#spring.datasource.password=${DATABASE_PASSWORD:123456}
#spring.datasource.driver-class-name=org.postgresql.Driver
#
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ========================
# Java Mail Config
# ========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:system250812@gmail.com}
spring.mail.password=${MAIL_PASSWORD:npxd todq evpd xmye}
spring.mail.protocol=smtp
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.timeout=8000
spring.mail.test-connection=true